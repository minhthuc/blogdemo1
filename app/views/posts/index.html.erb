<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <%= render @posts %>
    </div>
  </div>

  <div class="row ">
    <%= paginate @posts %>
  </div>
</div>

<%= link_to "New Post", new_post_path %>
