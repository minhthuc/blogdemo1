<%= form_for [post, comment]  do |f| %>
  <%= hidden_field_tag "comment[user_id]", current_user.id %>
  <%= f.text_field :content, class: "form-control" %>
  <%= f.submit t"devise.passwords.new.comment" %>
<% end %>
